// Given an array, return an array that has been reversed
// without using the reverse method

// Example -
// reverseArr([1,2,3,4,5]) => [5,4,3,2,1]

// Bonus - Use es6 array destructuring and don't create a new array

// Can solve as you like
function reverseNewArr(arr) {
}

// Try to use es 6 destructuring and 
// return the original array after you have reversed it
function reverseSameArr(arr) {

}

module.exports = {reverseNewArr, reverseSameArr};