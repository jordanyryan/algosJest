// Implement Binary Search

// Given a sorted array and a key, check to see if 
// the key is in the array

// Binary search works by splitting the array in half,
// comparing both halves with the key,
// and then using the side you need to continue
// your search

// Example -
// binarySearch([1,2,3,4,5], 2) => true 
// binarySearch([1,2,3,5,6], 4) => false
// binarySearch([2,4,7,9,11,12,14,17,23,43,56,80], 15) => false

function binarySearch(arr, key) {
}

module.exports = binarySearch;