const mergeArrays = require('./index');

// test("Tests to see that mergeArrays is a function", () => {
//   expect(typeof mergeArrays).toEqual(true);
// });

// test("Tests to see that mergeArrays returns an array", () => {
//   expect(Array.isArray(mergeArrays([1,3,4],[2,3,5]))).toEqual(true);
// });

// test("Expects mergeArrays to successfully merge and sort 2 sorted arrays", () => {
//   expect(mergeArrays([1,3,4],[2,3,5])).toEqual([1,2,3,3,4,5]);
// });