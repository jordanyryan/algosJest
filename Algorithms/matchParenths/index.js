// Given a string, check if opening parentheses 
// match with the closing parentheses
// If a string has correct positioning of opening
// and closing parentheses, return true
// if it doesn't return false


// Example -
// const str1 = "((a+b) * (a-b) + (x - 3))"
// matchParenths(str1) ==> true
// all parentheses have a closing parentheses that is correct

// const str2 = "((a+b))) + (a*b)"
// matchParenths(str2) ==> false
// There are more closing parentheses than opening, and 
// the parentheses close too early

function matchParenths(str) {
}

module.exports = matchParenths;